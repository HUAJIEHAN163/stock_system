# 🧠 智能增量更新方案

## 🎯 核心思想

通过智能分析数据缺失情况，实现精准的增量更新，而不是简单的时间范围更新。

## 📊 智能增量更新策略

### 1. 数据完整性检测

```python
# 数据完整性检测配置
DATA_INTEGRITY_CONFIG = {
    'daily_basic': {
        'expected_stock_count': 5000,  # 预期股票数量
        'missing_threshold': 0.05,     # 缺失率阈值5%
        'check_days': 30,              # 检查最近30个交易日
        'full_update_threshold': 0.20   # 超过20%缺失则全量更新该日
    },
    'index_daily': {
        'expected_index_count': 300,
        'missing_threshold': 0.03,
        'check_days': 30,
        'full_update_threshold': 0.15
    }
}
```

### 2. 缺失数据检测算法

```python
def detect_missing_data(table_name, check_days=30):
    """
    检测缺失数据
    返回: {
        'missing_dates': ['20241201', '20241202'],
        'partial_dates': {
            '20241203': {'missing_count': 250, 'total_expected': 5000}
        }
    }
    """
    
    # 1. 获取最近N个交易日
    trade_dates = get_recent_trade_dates(check_days)
    
    # 2. 获取活跃股票列表
    active_stocks = get_active_stocks()
    expected_count = len(active_stocks)
    
    missing_info = {
        'missing_dates': [],      # 完全缺失的日期
        'partial_dates': {},      # 部分缺失的日期
        'complete_dates': []      # 数据完整的日期
    }
    
    for trade_date in trade_dates:
        # 检查该日期的数据完整性
        actual_count = count_records_for_date(table_name, trade_date)
        
        if actual_count == 0:
            # 完全缺失
            missing_info['missing_dates'].append(trade_date)
        elif actual_count < expected_count:
            # 部分缺失
            missing_count = expected_count - actual_count
            missing_rate = missing_count / expected_count
            
            config = DATA_INTEGRITY_CONFIG[table_name]
            
            if missing_rate > config['missing_threshold']:
                missing_info['partial_dates'][trade_date] = {
                    'missing_count': missing_count,
                    'total_expected': expected_count,
                    'missing_rate': missing_rate,
                    'need_full_update': missing_rate > config['full_update_threshold']
                }
        else:
            # 数据完整
            missing_info['complete_dates'].append(trade_date)
    
    return missing_info
```

### 3. 智能更新决策

```python
def create_update_plan(missing_info, table_name):
    """
    根据缺失情况制定更新计划
    """
    update_plan = {
        'full_update_dates': [],      # 需要全量更新的日期
        'partial_update_dates': [],   # 需要部分更新的日期
        'missing_stocks_by_date': {}  # 每个日期缺失的具体股票
    }
    
    # 1. 处理完全缺失的日期
    update_plan['full_update_dates'].extend(missing_info['missing_dates'])
    
    # 2. 处理部分缺失的日期
    for date, info in missing_info['partial_dates'].items():
        if info['need_full_update']:
            # 缺失率过高，全量更新该日
            update_plan['full_update_dates'].append(date)
        else:
            # 缺失率较低，只更新缺失的股票
            update_plan['partial_update_dates'].append(date)
            missing_stocks = find_missing_stocks_for_date(table_name, date)
            update_plan['missing_stocks_by_date'][date] = missing_stocks
    
    return update_plan
```

### 4. 缺失股票识别

```python
def find_missing_stocks_for_date(table_name, trade_date):
    """
    找出指定日期缺失数据的股票
    """
    # 获取所有活跃股票
    all_stocks = get_active_stocks_for_date(trade_date)
    
    # 获取已有数据的股票
    existing_stocks = get_existing_stocks_for_date(table_name, trade_date)
    
    # 计算缺失的股票
    missing_stocks = set(all_stocks) - set(existing_stocks)
    
    return list(missing_stocks)

def get_active_stocks_for_date(trade_date):
    """
    获取指定日期的活跃股票列表（排除停牌、退市等）
    """
    conn = sqlite3.connect("database/stock_data.db")
    cursor = conn.cursor()
    
    # 查询在该日期前上市且未退市的股票
    cursor.execute("""
        SELECT ts_code FROM stock_basic 
        WHERE list_date <= ? 
        AND (delist_date IS NULL OR delist_date > ?)
    """, (trade_date, trade_date))
    
    stocks = [row[0] for row in cursor.fetchall()]
    conn.close()
    
    return stocks
```

## 🔄 数据覆盖机制

### 1. 三种覆盖策略

```python
# 数据覆盖策略配置
OVERRIDE_STRATEGIES = {
    'full': {
        'method': 'DELETE + INSERT',
        'description': '删除该日期所有数据后重新插入',
        'use_case': '数据质量问题严重，需要完全重新获取',
        'transaction': True
    },
    'partial': {
        'method': 'DELETE WHERE + INSERT', 
        'description': '只删除有问题股票的数据后重新插入',
        'use_case': '少数股票数据异常，大部分数据正常',
        'transaction': True
    },
    'missing_only': {
        'method': 'INSERT OR IGNORE',
        'description': '只添加缺失数据，不影响已有数据',
        'use_case': '数据基本完整，只有少量缺失',
        'transaction': True
    }
}
```

### 2. 事务安全保证

```python
def _full_date_override(self, table_name, trade_date):
    """全量覆盖 - 确保数据完全替换"""
    conn = None
    try:
        conn = self.db_manager.get_connection()
        
        # 1. 开始事务
        conn.execute("BEGIN TRANSACTION")
        
        # 2. 删除该日期所有数据
        delete_sql = f"DELETE FROM {table_name} WHERE trade_date = ?"
        cursor = conn.cursor()
        cursor.execute(delete_sql, (trade_date,))
        deleted_count = cursor.rowcount
        
        # 3. 获取并插入新数据
        df = self.pro.daily(trade_date=trade_date)
        if not df.empty:
            df['src'] = 'incremental_override'
            df.to_sql(table_name, conn, if_exists='append', index=False)
            
            # 4. 提交事务
            conn.commit()
            return True, len(df), f"全量覆盖成功：删除{deleted_count}条，新增{len(df)}条"
        else:
            conn.rollback()
            return False, 0, "API返回空数据，回滚事务"
            
    except Exception as e:
        if conn:
            conn.rollback()
        return False, 0, f"全量覆盖失败: {str(e)}"
    finally:
        if conn:
            conn.close()
```

### 3. 智能决策入口

```python
def ensure_data_override(self, table_name, trade_date, force_override=False):
    """确保当日数据可以被覆盖的主入口方法"""
    
    if force_override:
        # 强制全量覆盖
        return self._full_date_override(table_name, trade_date)
    
    # 智能决策覆盖策略
    update_type, reason = self.smart_update_decision(table_name, trade_date)
    
    if update_type == 'full':
        return self._full_date_override(table_name, trade_date)
    elif update_type == 'partial':
        return self._partial_date_override(table_name, trade_date)
    else:
        return self._missing_only_update(table_name, trade_date)
```

## 🔄 增量更新执行流程

### 1. 数据完整性分析表

```sql
-- 数据完整性分析表
CREATE TABLE data_integrity_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    table_name TEXT,
    trade_date TEXT,
    expected_count INTEGER,
    actual_count INTEGER,
    missing_count INTEGER,
    missing_rate REAL,
    check_time TEXT,
    status TEXT,  -- 'complete', 'partial', 'missing'
    UNIQUE(table_name, trade_date, check_time)
);

-- 更新任务表
CREATE TABLE update_tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_type TEXT,  -- 'full_date', 'partial_date', 'missing_stocks'
    table_name TEXT,
    trade_date TEXT,
    target_stocks TEXT,  -- JSON格式的股票列表
    status TEXT,     -- 'pending', 'running', 'completed', 'failed'
    created_time TEXT,
    start_time TEXT,
    end_time TEXT,
    error_msg TEXT
);
```

### 2. 智能更新主流程

```python
class SmartIncrementalUpdater:
    def __init__(self):
        self.config = DATA_INTEGRITY_CONFIG
        
    def run_smart_update(self, tables=['daily_basic', 'index_daily']):
        """
        执行智能增量更新
        """
        update_summary = {
            'total_tasks': 0,
            'completed_tasks': 0,
            'failed_tasks': 0,
            'updated_records': 0
        }
        
        for table_name in tables:
            print(f"🔍 分析 {table_name} 数据完整性...")
            
            # 1. 检测缺失数据
            missing_info = detect_missing_data(table_name)
            
            # 2. 制定更新计划
            update_plan = create_update_plan(missing_info, table_name)
            
            # 3. 记录分析结果
            self.log_integrity_analysis(table_name, missing_info)
            
            # 4. 执行更新任务
            task_results = self.execute_update_plan(table_name, update_plan)
            
            # 5. 更新统计
            update_summary['total_tasks'] += task_results['total']
            update_summary['completed_tasks'] += task_results['completed']
            update_summary['failed_tasks'] += task_results['failed']
            update_summary['updated_records'] += task_results['records']
        
        return update_summary
    
    def execute_update_plan(self, table_name, update_plan):
        """
        执行更新计划
        """
        results = {'total': 0, 'completed': 0, 'failed': 0, 'records': 0}
        
        # 1. 处理全量更新日期
        for date in update_plan['full_update_dates']:
            task_id = self.create_update_task('full_date', table_name, date)
            success, records = self.update_full_date_data(table_name, date)
            
            results['total'] += 1
            if success:
                results['completed'] += 1
                results['records'] += records
                self.update_task_status(task_id, 'completed')
            else:
                results['failed'] += 1
                self.update_task_status(task_id, 'failed')
        
        # 2. 处理部分更新日期
        for date in update_plan['partial_update_dates']:
            missing_stocks = update_plan['missing_stocks_by_date'][date]
            task_id = self.create_update_task('partial_date', table_name, date, missing_stocks)
            success, records = self.update_partial_date_data(table_name, date, missing_stocks)
            
            results['total'] += 1
            if success:
                results['completed'] += 1
                results['records'] += records
                self.update_task_status(task_id, 'completed')
            else:
                results['failed'] += 1
                self.update_task_status(task_id, 'failed')
        
        return results
```

### 3. 具体更新实现

```python
def update_full_date_data(self, table_name, trade_date):
    """
    全量更新指定日期的数据
    """
    try:
        if table_name == 'daily_basic':
            # 获取该日所有股票的行情数据
            df = pro.daily(trade_date=trade_date)
        elif table_name == 'index_daily':
            # 获取该日所有指数的行情数据
            df = pro.index_daily(trade_date=trade_date)
        
        if df.empty:
            return False, 0
        
        # 删除该日期的旧数据
        self.delete_date_data(table_name, trade_date)
        
        # 插入新数据
        records_count = self.insert_data(table_name, df)
        
        return True, records_count
        
    except Exception as e:
        print(f"❌ 全量更新失败 {table_name} {trade_date}: {e}")
        return False, 0

def update_partial_date_data(self, table_name, trade_date, missing_stocks):
    """
    部分更新指定日期的缺失股票数据
    """
    try:
        updated_records = 0
        
        # 分批处理缺失的股票
        batch_size = 50
        for i in range(0, len(missing_stocks), batch_size):
            batch_stocks = missing_stocks[i:i+batch_size]
            
            if table_name == 'daily_basic':
                # 获取指定股票的行情数据
                for ts_code in batch_stocks:
                    df = pro.daily(ts_code=ts_code, trade_date=trade_date)
                    if not df.empty:
                        self.insert_data(table_name, df)
                        updated_records += len(df)
            
        return True, updated_records
        
    except Exception as e:
        print(f"❌ 部分更新失败 {table_name} {trade_date}: {e}")
        return False, 0
```

## 📊 优化建议

### 1. 性能优化

```python
# 缓存机制
CACHE_CONFIG = {
    'active_stocks': {
        'ttl': 3600,  # 1小时缓存
        'key': 'active_stocks_list'
    },
    'trade_dates': {
        'ttl': 86400,  # 24小时缓存
        'key': 'recent_trade_dates'
    }
}

# 并发处理
CONCURRENT_CONFIG = {
    'max_workers': 5,           # 最大并发数
    'batch_size': 50,           # 每批处理股票数
    'api_rate_limit': 200       # API调用频率限制
}
```

### 2. 监控和告警

```python
# 数据质量监控
QUALITY_MONITORS = {
    'missing_rate_alert': 0.10,     # 缺失率超过10%告警
    'update_failure_alert': 3,      # 连续3次更新失败告警
    'data_delay_alert': 2           # 数据延迟超过2天告警
}

def check_data_quality():
    """
    数据质量检查
    """
    alerts = []
    
    for table_name in ['daily_basic', 'index_daily']:
        # 检查最近数据的完整性
        latest_missing = detect_missing_data(table_name, check_days=5)
        
        if latest_missing['missing_dates']:
            alerts.append(f"⚠️ {table_name} 存在完全缺失的交易日: {latest_missing['missing_dates']}")
        
        for date, info in latest_missing['partial_dates'].items():
            if info['missing_rate'] > QUALITY_MONITORS['missing_rate_alert']:
                alerts.append(f"⚠️ {table_name} {date} 缺失率过高: {info['missing_rate']:.2%}")
    
    return alerts
```

### 3. 覆盖策略选择

```python
# 覆盖策略决策逻辑
def smart_update_decision(self, table_name, trade_date):
    """智能决策更新策略"""
    missing_stocks = self._find_missing_stocks(table_name, trade_date)
    problematic_stocks = self._find_problematic_stocks(table_name, trade_date)
    
    total_expected = len(self._get_active_stocks(trade_date))
    missing_rate = len(missing_stocks) / total_expected if total_expected > 0 else 0
    problem_rate = len(problematic_stocks) / total_expected if total_expected > 0 else 0
    
    if missing_rate > 0.2 or problem_rate > 0.1:
        return 'full', f"缺失率{missing_rate:.1%}，问题率{problem_rate:.1%}，建议全量更新"
    elif missing_stocks or problematic_stocks:
        return 'partial', f"需要更新{len(set(missing_stocks + problematic_stocks))}只股票"
    else:
        return 'none', "数据完整，无需更新"
```

### 4. 用户界面增强

```python
# GUI中的智能更新状态显示
class SmartUpdateStatusWidget:
    def show_analysis_result(self, missing_info):
        """
        显示数据分析结果
        """
        status_text = []
        
        if missing_info['missing_dates']:
            status_text.append(f"🔴 完全缺失: {len(missing_info['missing_dates'])} 个交易日")
        
        if missing_info['partial_dates']:
            partial_count = len(missing_info['partial_dates'])
            status_text.append(f"🟡 部分缺失: {partial_count} 个交易日")
        
        if missing_info['complete_dates']:
            complete_count = len(missing_info['complete_dates'])
            status_text.append(f"🟢 数据完整: {complete_count} 个交易日")
        
        return "\n".join(status_text)
```

## 🎯 实施优势

### 1. 数据覆盖保证
- **事务安全**: 所有覆盖操作都在事务中执行
- **原子操作**: DELETE + INSERT 确保数据完全替换
- **自动回滚**: 出错时自动回滚，保证数据一致性
- **数据标记**: src字段标记数据来源，便于追踪

### 2. 效率提升
- **精准更新**: 只更新真正缺失的数据
- **API节约**: 避免重复获取已有数据
- **时间节省**: 智能跳过完整数据
- **策略优化**: 根据数据质量自动选择最优策略

### 3. 数据质量
- **完整性保证**: 确保数据无遗漏
- **一致性检查**: 自动发现数据异常
- **质量监控**: 实时监控数据状态
- **覆盖验证**: 确保当日数据能被正确覆盖

### 4. 用户体验
- **透明进度**: 清晰显示更新内容
- **智能决策**: 自动选择最优更新策略
- **错误恢复**: 支持失败任务重试
- **强制模式**: 支持强制覆盖选项

## 📋 使用示例

```python
# 智能覆盖（推荐）
updater = IncrementalUpdater()
success, records, message = updater.ensure_data_override('daily_basic', '20241201')

# 强制全量覆盖
success, records, message = updater.ensure_data_override('daily_basic', '20241201', force_override=True)

# 指定覆盖类型
success, records, message = updater.update_date_data_with_override('daily_basic', '20241201', 'full')
```

这个智能增量更新方案不仅比简单的30天更新要高效得多，还确保了当日数据能够被安全、完整地覆盖。