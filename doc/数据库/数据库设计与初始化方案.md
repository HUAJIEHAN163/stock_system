# 📊 数据库设计与初始化方案

## 🎯 概述

本文档记录了股票分析系统的数据库设计方案和数据初始化策略，基于Tushare API分类和项目需求制定。

## 📋 当前状态分析

### ✅ 已实现功能
1. **数据初始化时间段**: 
   - 交易日历: 过去2年到当前日期 (last_2_years)
   - 股票基本信息: 获取所有在市股票 (无时间限制)
   - 日线行情: 过去1年 (last_1_year) - 已优化
   - 周线行情: 过去1年 (last_1_year)
   - 月线行情: 过去2年 (last_2_years)
   - 复权因子: 过去1年 (last_1_year)
   - 指数每日指标: 过去1年 (last_1_year)

2. **批量API策略**: ✅ 已按要求设计
   - 数据初始化只获取批量API数据
   - 逐只股票数据需要时实时获取

3. **本地数据库**: ✅ 永久保存SQLite数据库

4. **数据初始化按键**: ✅ 基础功能已实装
   - UI界面和基础逻辑完成
   - 支持Token验证、进度显示

### ⚠️ 需要改进的问题
1. **数据库字段变化**: ✅ 已优化 - 基于测试报告调整字段和数据源标识
2. **API权限管理**: ✅ 已优化 - 明确区分成功/失败API，跳过权限不足的API
3. **时间范围优化**: ✅ 已优化 - 根据数据类型调整合理的时间区间
4. **数据源区分**: 🔄 进行中 - 添加src字段区分不同来源数据
5. **性能优化**: ✅ 已优化 - 采用测试验证的高效查询策略

---

## 🗄️ 数据库设计方案

### 📊 第一阶段：核心批量数据表

#### 1. 基础数据表

```sql
-- 股票基本信息 (已实现)
CREATE TABLE stock_basic (
    ts_code TEXT PRIMARY KEY,
    symbol TEXT,
    name TEXT,
    area TEXT,
    industry TEXT,
    market TEXT,
    list_date TEXT,
    update_time TEXT
);

-- 交易日历 (已实现)
CREATE TABLE trade_calendar (
    cal_date TEXT PRIMARY KEY,
    is_open INTEGER,
    pretrade_date TEXT
);

-- 指数基本信息 (新增)
CREATE TABLE index_basic (
    ts_code TEXT PRIMARY KEY,
    name TEXT,
    market TEXT,
    publisher TEXT,
    category TEXT,
    base_date TEXT,
    base_point REAL,
    list_date TEXT,
    update_time TEXT
);

-- 行业分类 (新增，区分数据源)
CREATE TABLE industry_classify (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    industry_code TEXT,
    industry_name TEXT,
    level INTEGER,
    parent_code TEXT,
    src TEXT,  -- 'SW'(申万), 'ZX'(中信), 'THS'(同花顺), 'TS'(Tushare)
    test_status TEXT,  -- 'SUCCESS', 'FAILED', 'PENDING'
    update_time TEXT,
    UNIQUE(industry_code, src)
);

-- 上市公司基本信息 (基于测试报告新增)
CREATE TABLE stock_company (
    ts_code TEXT PRIMARY KEY,
    com_name TEXT,
    chairman TEXT,
    manager TEXT,
    secretary TEXT,
    reg_capital REAL,
    setup_date TEXT,
    province TEXT,
    city TEXT,
    website TEXT,
    email TEXT,
    employees INTEGER,
    main_business TEXT,
    update_time TEXT
);

-- IPO新股列表 (基于测试报告新增)
CREATE TABLE new_share (
    ts_code TEXT PRIMARY KEY,
    sub_code TEXT,
    name TEXT,
    ipo_date TEXT,
    issue_date TEXT,
    amount REAL,
    market_amount REAL,
    price REAL,
    pe REAL,
    limit_amount REAL,
    funds REAL,
    ballot REAL,
    update_time TEXT
);

-- 复权因子 (基于测试报告新增)
CREATE TABLE adj_factor (
    ts_code TEXT,
    trade_date TEXT,
    adj_factor REAL,
    update_time TEXT,
    PRIMARY KEY (ts_code, trade_date)
);

-- 指数每日基本指标 (基于测试报告新增)
CREATE TABLE index_dailybasic (
    ts_code TEXT,
    trade_date TEXT,
    total_mv REAL,
    float_mv REAL,
    total_share REAL,
    float_share REAL,
    free_share REAL,
    turnover_rate REAL,
    turnover_rate_f REAL,
    pe REAL,
    pe_ttm REAL,
    pb REAL,
    update_time TEXT,
    PRIMARY KEY (ts_code, trade_date)
);

-- 概念分类 (新增)
CREATE TABLE concept_classify (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    concept_code TEXT,
    concept_name TEXT,
    src TEXT,  -- 数据源标识
    update_time TEXT,
    UNIQUE(concept_code, src)
);
```

#### 2. 行情数据表

```sql
-- A股日线行情 (核心表)
CREATE TABLE daily_basic (
    ts_code TEXT,
    trade_date TEXT,
    open REAL,
    high REAL,
    low REAL,
    close REAL,
    pre_close REAL,
    change REAL,
    pct_chg REAL,
    vol REAL,
    amount REAL,
    turnover_rate REAL,
    volume_ratio REAL,
    pe REAL,
    pb REAL,
    update_time TEXT,
    PRIMARY KEY (ts_code, trade_date)
);

-- 指数日线行情
CREATE TABLE index_daily (
    ts_code TEXT,
    trade_date TEXT,
    open REAL,
    high REAL,
    low REAL,
    close REAL,
    pre_close REAL,
    change REAL,
    pct_chg REAL,
    vol REAL,
    amount REAL,
    update_time TEXT,
    PRIMARY KEY (ts_code, trade_date)
);

-- 周线行情
CREATE TABLE weekly_basic (
    ts_code TEXT,
    trade_date TEXT,
    open REAL,
    high REAL,
    low REAL,
    close REAL,
    pre_close REAL,
    change REAL,
    pct_chg REAL,
    vol REAL,
    amount REAL,
    update_time TEXT,
    PRIMARY KEY (ts_code, trade_date)
);

-- 月线行情
CREATE TABLE monthly_basic (
    ts_code TEXT,
    trade_date TEXT,
    open REAL,
    high REAL,
    low REAL,
    close REAL,
    pre_close REAL,
    change REAL,
    pct_chg REAL,
    vol REAL,
    amount REAL,
    update_time TEXT,
    PRIMARY KEY (ts_code, trade_date)
);
```

### 🔧 第二阶段：扩展数据表

#### 3. 成分股数据

```sql
-- 指数成分股 (统一表，用type区分)
CREATE TABLE index_weight (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    index_code TEXT,
    con_code TEXT,
    trade_date TEXT,
    weight REAL,
    in_date TEXT,
    out_date TEXT,
    index_type TEXT,  -- 'HS300', 'SZ50', 'ZZ500', 'ZZ1000', 'KC50', 'CYB'等
    update_time TEXT,
    UNIQUE(index_code, con_code, trade_date)
);
```

#### 4. 资金流向数据

```sql
-- 沪深股通资金流向
CREATE TABLE hsgt_flow (
    trade_date TEXT PRIMARY KEY,
    ggt_ss REAL,      -- 港股通(沪)
    ggt_sz REAL,      -- 港股通(深)
    hgt REAL,         -- 沪股通
    sgt REAL,         -- 深股通
    north_money REAL, -- 北向资金
    south_money REAL, -- 南向资金
    update_time TEXT
);

-- 沪深股通十大成交股
CREATE TABLE hsgt_top10 (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    trade_date TEXT,
    ts_code TEXT,
    name TEXT,
    close REAL,
    change REAL,
    rank INTEGER,
    market_type TEXT,  -- 'SH', 'SZ'
    amount REAL,
    net_amount REAL,
    update_time TEXT,
    UNIQUE(trade_date, ts_code, market_type)
);
```

### 📈 索引优化

```sql
-- 核心索引
CREATE INDEX idx_daily_code_date ON daily_basic(ts_code, trade_date);
CREATE INDEX idx_daily_date ON daily_basic(trade_date);
CREATE INDEX idx_index_daily_code_date ON index_daily(ts_code, trade_date);
CREATE INDEX idx_stock_basic_market ON stock_basic(market);
CREATE INDEX idx_stock_basic_industry ON stock_basic(industry);
CREATE INDEX idx_trade_calendar_date ON trade_calendar(cal_date);
CREATE INDEX idx_industry_src ON industry_classify(src);
CREATE INDEX idx_concept_src ON concept_classify(src);
```

---

## 🚀 数据初始化策略

### 📊 分批初始化方案

#### 第1批：基础数据（必须，优先级最高）

```python
# 立即初始化的API
BATCH_1_APIS = {
    'stock_basic': {
        'api_name': 'stock_basic',
        'table': 'stock_basic',
        'params': {'exchange': '', 'list_status': 'L'},
        'fields': 'ts_code,symbol,name,area,industry,market,list_date'
    },
    'trade_cal': {
        'api_name': 'trade_cal', 
        'table': 'trade_calendar',
        'params': {'exchange': ''},
        'time_range': 'last_2_years'
    },
    'index_basic': {
        'api_name': 'index_basic',
        'table': 'index_basic', 
        'params': {'market': 'SSE,SZSE'}
    },
    'sw_daily': {
        'api_name': 'sw_daily',
        'table': 'industry_classify',
        'params': {'level': 'L1,L2,L3'},
        'src': 'SW'
    },
    'concept': {
        'api_name': 'concept',
        'table': 'concept_classify',
        'params': {'src': 'ts'},
        'src': 'TS'
    }
}
```

#### 第2批：历史行情（重要，分时间段）

```python
# 分时间段初始化的API
BATCH_2_APIS = {
    'daily': {
        'api_name': 'daily',
        'table': 'daily_basic',
        'time_range': 'last_2_years',
        'batch_size': 100  # 每次处理100只股票
    },
    'index_daily': {
        'api_name': 'index_daily',
        'table': 'index_daily',
        'time_range': 'last_2_years'
    },
    'weekly': {
        'api_name': 'weekly',
        'table': 'weekly_basic', 
        'time_range': 'last_2_years'
    },
    'monthly': {
        'api_name': 'monthly',
        'table': 'monthly_basic',
        'time_range': 'last_5_years'
    }
}
```

#### 第3批：成分股数据（补充，按需）

```python
# 按需初始化的API
BATCH_3_APIS = {
    'hs300_weight': {
        'api_name': 'hs300_weight',
        'table': 'index_weight',
        'index_type': 'HS300'
    },
    'sz50_weight': {
        'api_name': 'sz50_weight', 
        'table': 'index_weight',
        'index_type': 'SZ50'
    },
    'zz500_weight': {
        'api_name': 'zz500_weight',
        'table': 'index_weight', 
        'index_type': 'ZZ500'
    }
}
```

### ⏰ 时间范围配置

```python
# 时间范围配置
TIME_RANGES = {
    'last_1_year': lambda: (
        (datetime.now() - timedelta(days=365)).strftime('%Y%m%d'),
        datetime.now().strftime('%Y%m%d')
    ),
    'last_2_years': lambda: (
        (datetime.now() - timedelta(days=730)).strftime('%Y%m%d'),
        datetime.now().strftime('%Y%m%d')
    ),
    'last_5_years': lambda: (
        (datetime.now() - timedelta(days=1825)).strftime('%Y%m%d'),
        datetime.now().strftime('%Y%m%d')
    ),
    'custom': lambda start, end: (start, end)
}
```

### 🔄 数据源标识方案

```python
# 数据源映射
DATA_SOURCE_MAPPING = {
    'tushare': 'TS',
    'tudata': 'TD',
    'sw': 'SW',        # 申万
    'zx': 'ZX',        # 中信
    'ths': 'THS',      # 同花顺
    'concept_ts': 'TS', # Tushare概念
    'concept_ths': 'THS' # 同花顺概念
}

# 重复数据处理策略
DUPLICATE_DATA_STRATEGY = {
    'industry_classify': {
        'sw': '申万行业分类',
        'zx': '中信行业分类', 
        'ths': '同花顺行业分类'
    },
    'concept_classify': {
        'ts': 'Tushare概念分类',
        'ths': '同花顺概念分类'
    }
}
```

---

## 💾 存储优化方案

### 1. 分表策略

```python
# 按年份分表存储大数据量表
TABLE_PARTITION_STRATEGY = {
    'daily_basic': 'yearly',      # 按年分表
    'index_daily': 'yearly',     # 按年分表
    'weekly_basic': 'single',    # 单表存储
    'monthly_basic': 'single'    # 单表存储
}

# 分表命名规则
def get_partition_table_name(base_table, year):
    return f"{base_table}_{year}"
```

### 2. 数据压缩

```sql
-- SQLite压缩配置
PRAGMA journal_mode = WAL;
PRAGMA synchronous = NORMAL;
PRAGMA cache_size = 10000;
PRAGMA temp_store = memory;
```

### 3. 定期清理策略

```python
# 数据清理配置
DATA_CLEANUP_POLICY = {
    'daily_basic': {
        'keep_years': 5,
        'cleanup_frequency': 'monthly'
    },
    'minute_data': {  # 未来分钟数据
        'keep_years': 1,
        'cleanup_frequency': 'weekly'
    }
}
```

---

## 🔧 实现建议

### 1. 配置化管理

```python
# API配置文件 (config/api_config.json)
{
    "batch_1": {
        "priority": 1,
        "apis": ["stock_basic", "trade_cal", "index_basic"],
        "required": true
    },
    "batch_2": {
        "priority": 2, 
        "apis": ["daily", "index_daily"],
        "required": false,
        "time_dependent": true
    }
}
```

### 2. 断点续传机制

```python
# 初始化进度表
CREATE TABLE init_progress (
    batch_name TEXT,
    api_name TEXT,
    status TEXT,  -- 'pending', 'running', 'completed', 'failed'
    start_time TEXT,
    end_time TEXT,
    progress INTEGER,
    error_msg TEXT,
    PRIMARY KEY (batch_name, api_name)
);
```

### 3. 数据校验

```python
# 数据完整性检查
VALIDATION_RULES = {
    'stock_basic': {
        'min_records': 4000,
        'required_fields': ['ts_code', 'name', 'market']
    },
    'daily_basic': {
        'date_continuity': True,
        'price_validation': True
    }
}
```

### 4. 错误重试机制

```python
# 重试配置
RETRY_CONFIG = {
    'max_retries': 3,
    'retry_delay': [1, 5, 10],  # 秒
    'retry_conditions': ['network_error', 'api_limit', 'timeout']
}
```

---

## 📊 预估数据量

### 存储空间预估

| 数据类型 | 记录数量 | 单条大小 | 总大小 | 备注 |
|---------|---------|---------|--------|------|
| 股票基本信息 | ~5,000 | 200B | 1MB | 基础数据 |
| 交易日历 | ~500/年 | 50B | 50KB/年 | 2年约100KB |
| 日线行情 | 250万条 | 150B | 375MB | 5000股票×2年 |
| 指数行情 | 15万条 | 120B | 18MB | 300指数×2年 |
| 成分股数据 | 10万条 | 100B | 10MB | 各指数成分股 |
| **总计** | - | - | **~400MB** | 2年历史数据 |

### 性能指标

- **初始化时间**: 预计30-60分钟（首次）
- **增量更新**: 预计5-10分钟（每日）
- **查询性能**: <100ms（单股票年度数据）
- **并发支持**: 支持10个并发查询

---

## 🎯 开发优先级

### Phase 1: 基础数据库 (1周) - ✅ 已完成
1. ✅ 完善现有数据库表结构 - 基于测试报告优化
2. ✅ 实现第1批API数据初始化 - 10个成功API
3. ✅ 添加数据源标识字段 - src字段区分来源
4. ✅ 基础数据校验 - 基于实际测试数据调整阈值
5. ✅ API权限管理 - 跳过权限不足的API
6. ✅ 时间范围优化 - 根据数据类型调整合理区间
7. ✅ 数据覆盖机制 - 初始化时清空旧数据避免冲突
8. ✅ 问题诊断流程 - 基于实际测试的问题修复方法

### Phase 2: 行情数据 (2周) - ✅ 已完成
1. ✅ 实现日线行情数据初始化 - 多策略支持
2. ✅ 添加指数行情数据 - index_dailybasic等
3. ✅ 实现分批处理机制 - 批次大小优化
4. ✅ 优化存储性能 - 数据类型处理和清理
5. ✅ 唯一约束处理 - 重复数据覆盖机制
6. ✅ 错误处理增强 - 针对性问题修复

### Phase 3: 扩展功能 (1周) - 🔄 进行中
1. ⏳ 成分股数据集成
2. ⏳ 资金流向数据
3. 🔄 智能增量更新 - 基于数据完整性检测
4. 🔄 数据质量监控 - 缺失数据自动检测
5. ⏳ 断点续传功能
6. ⏳ 数据清理策略

---

*文档创建时间: 2024年12月*  
*最后更新: 2024年12月*  
*版本: v1.0*